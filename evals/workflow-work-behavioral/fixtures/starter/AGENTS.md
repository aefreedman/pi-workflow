# Synthetic Work Eval Workspace

- Implement only the scoped request in this fresh temporary workspace.
- `importer.ts` is a mutable implementation target; source behavior must retain three attempts.
- `WORK-PLAN.md`, when present, is authoritative and read-only unless the prompt explicitly requests an edit.
- Do not use commits, VCS, network, credentials, tracker, publication, branch, pull-request, or review actions.
